<?xml version="1.0" encoding="UTF-8"?>
<!--
  frame-me 共享日志模板：不会被 Spring Boot 自动加载（文件名非 logback-spring.xml）。
  应用在自己的 logback-spring.xml 中通过 <include resource="logback-frame-me.xml"/> 显式引入，
  再按需追加业务专属 logger / appender。
-->
<included>

    <!-- 属性定义 -->
    <property name="LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n"/>
    <property name="COLOR_LOG_PATTERN"
              value="%d{yyyy-MM-dd HH:mm:ss.SSS} %magenta([%thread]) %highlight(%-5level) %cyan(%logger{36}) - %msg%n"/>
    <property name="LOG_FILE_PATH" value="${LOG_PATH:-logs}/${LOG_FILE:-frame-me}"/>

    <!-- 控制台输出 -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${COLOR_LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 文件输出，按天滚动 -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_FILE_PATH}.log</file>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <fileNamePattern>${LOG_FILE_PATH}.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <maxHistory>30</maxHistory>
            <maxFileSize>100MB</maxFileSize>
            <totalSizeCap>3GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <pattern>${LOG_PATTERN}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- 异步文件输出，减少 IO 阻塞 -->
    <appender name="ASYNC_FILE" class="ch.qos.logback.classic.AsyncAppender">
        <appender-ref ref="FILE"/>
        <queueSize>2048</queueSize>
        <discardingThreshold>0</discardingThreshold>
        <neverBlock>true</neverBlock>
    </appender>

    <!-- 默认日志级别 -->
    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
        <appender-ref ref="ASYNC_FILE"/>
    </root>

    <!-- 第三方框架日志级别 -->
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.apache.ibatis" level="WARN"/>
    <logger name="com.baomidou.mybatisplus" level="WARN"/>
    <logger name="com.mybatisflex" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="WARN"/>
    <logger name="com.alicp.jetcache" level="INFO"/>
    <logger name="org.springframework.web" level="INFO"/>
    <logger name="com.baomidou.mybatisplus.core.toolkit.Sequence" level="DEBUG"/>

    <!-- 本地/开发环境：彩色控制台 + 调试级别 -->
    <springProfile name="local,dev,default">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
        <logger name="com.frame.me" level="DEBUG"/>
    </springProfile>

    <!-- 测试环境：控制台 + 文件 -->
    <springProfile name="test">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
        <logger name="com.frame.me" level="DEBUG"/>
    </springProfile>

    <!-- 日常环境：控制台 + 文件，保留较详细的业务日志 -->
    <springProfile name="daily">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
        <logger name="com.frame.me" level="DEBUG"/>
    </springProfile>

    <!-- 预发环境：控制台 + 文件，保留较详细的业务日志 -->
    <springProfile name="pre">
        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
            <appender-ref ref="ASYNC_FILE"/>
        </root>
        <logger name="com.frame.me" level="INFO"/>
    </springProfile>

    <!-- 生产环境：仅输出文件，关闭控制台 -->
    <springProfile name="prod">
        <root level="INFO">
            <appender-ref ref="ASYNC_FILE"/>
        </root>
        <logger name="com.frame.me" level="INFO"/>
    </springProfile>

</included>
