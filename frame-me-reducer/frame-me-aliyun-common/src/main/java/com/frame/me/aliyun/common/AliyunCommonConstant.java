package com.frame.me.aliyun.common;

/**
 * 阿里云公共 SDK 底座占位常量类.
 *
 * <p>本模块为阿里云系列能力的公共底座：存放各阿里云服务（OSS / SMS / ECS 等）共用的
 * SDK 与公共依赖。具体能力由各 {@code frame-me-aliyun-starter-xxx} 模块按需引入本模块获得公共依赖.
 *
 * <p>本模块不带 starter 后缀、无自动装配（与 {@code frame-me-adapter-api} 同为公共底座定位）.
 * 当前为占位工程，暂无公共 SDK 依赖，后续按实际接入的阿里云服务补充.</p>
 *
 * @author frame-me
 */
public final class AliyunCommonConstant {

    private AliyunCommonConstant() {
    }
}
