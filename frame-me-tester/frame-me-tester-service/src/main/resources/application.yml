server:
  port: 9090

management:
  server:
    port: 9091

spring:
  application:
    name: frame-me-tester
  datasource:
    url: jdbc:mysql://localhost:3306/frame_me_test3?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: ME(O30Gx4FH/kvKUyuawsrt4skU0l/YzE8S5XsezegH9V9gEq92XGCFTqR58+n3O6L1)
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 10
  #    druid:
  #      stat-view-servlet:
  #        enabled: true
  #        url-pattern: /druid/*
  #        login-username: admin
  #        login-password: admin
  #      initial-size: 5
  #      min-idle: 5
  #      max-active: 20
  #      max-wait: 60000
  #      time-between-eviction-runs-millis: 60000
  #      min-evictable-idle-time-millis: 300000
  #      validation-query: SELECT 1
  #      test-while-idle: true
  #      test-on-borrow: false
  #      test-on-return: false
  #    dynamic:
  #      hikari:
  #        maximum-pool-size: 10
  #        minimum-idle: 10
  #      datasource:
  #        second:
  #          url: jdbc:mysql://localhost:3306/frame_me_test_2?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
  #          username: root
  #          password: ME(O30Gx4FH/kvKUyuawsrt4skU0l/YzE8S5XsezegH9V9gEq92XGCFTqR58+n3O6L1)
  #          driver-class-name: com.mysql.cj.jdbc.Driver
  jackson:
    default-property-inclusion: non_null
  data:
    redis:
      database: 0
      host: localhost
      port: 6379
  #      username: xx
  #      password: xx
  sql:
    init:
      mode: always
      schema-locations: classpath:schema.sql

#mybatis-plus:
#  mapper-locations: classpath*:/mapper/**/*.xml
#  configuration:
#    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
#    map-underscore-to-camel-case: true
#  global-config:
#    db-config:
#      logic-delete-field: deleted
#      logic-delete-value: 1
#      logic-not-delete-value: 0
#      id-type: assign_id

mybatis-flex:
  default-datasource-key: master
  mapper-locations: classpath*:/mapper/**/*.xml
  global-config:
    logic-delete-column: deleted
    normal-value-of-logic-delete: 0
    deleted-value-of-logic-delete: 1
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.slf4j.Slf4jImpl
  datasource:
    master:
      url: jdbc:mysql://localhost:3306/frame_me_test?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      username: root
      password: ME(O30Gx4FH/kvKUyuawsrt4skU0l/YzE8S5XsezegH9V9gEq92XGCFTqR58+n3O6L1)
      driver-class-name: com.mysql.cj.jdbc.Driver
      type: hikari
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 1
    #      type: druid
    #      initial-size: 20
    #      min-idle: 20
    #      max-active: 20
    #      max-wait: 60000
    #      time-between-eviction-runs-millis: 60000
    #      min-evictable-idle-time-millis: 300000
    #      validation-query: SELECT 1
    #      test-while-idle: true
    #      test-on-borrow: false
    #      test-on-return: false
    second:
      url: jdbc:mysql://localhost:3306/frame_me_test_2?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
      username: root
      password: ME(O30Gx4FH/kvKUyuawsrt4skU0l/YzE8S5XsezegH9V9gEq92XGCFTqR58+n3O6L1)
      driver-class-name: com.mysql.cj.jdbc.Driver
      type: hikari
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
      connection-test-query: SELECT 2

logging:
  file:
    path: ./logs
    name: ${spring.application.name}
  level:
    com.alicp.jetcache: INFO

springdoc:
  # /v3/api-docs
  api-docs:
    enabled: true
  # /swagger-ui.html
  swagger-ui:
    enabled: true

# 依赖通过 Maven profile "p6spy" 引入：mvn ... -Pp6spy
decorator:
  datasource:
    enabled: true
    p6spy:
      enable-logging: true

# sa-token 原生参数（官方 sa-token.* 配置路径，秒数 long 形式）；jwt 模式下被忽略
sa-token:
  token-name: satoken
  timeout: 604800        # 7 天（秒），token 绝对有效期
  active-timeout: 7200   # 2 小时（秒），闲置冻结窗口；-1 表示不限制
  # cookie:              # 原生 Cookie 属性（可选）：登录自动写 Cookie、登出清、续期刷（is-read-cookie 默认 true）
  #   domain: .example.com

me:
  auth:
    enabled: true
    jwt:
      enabled: true
      secret: me-jwt-secret-key-at-least-32-characters-long-ksndifkwenkfj
      issuer: me-tester
      access-token-expires: PT2H
      refresh-token-expires: P7D
      path: /base/auth
    whitelist:
      - /base/auth/login
      - /base/auth/refresh
      - /swagger-ui/**
      - /swagger-ui.html
      - /v3/api-docs/**
      - /v3/api-docs
      - /actuator/health
    sa-token:
      enabled: true
      path: /base/auth
      users:
        "1": admin
      roles:
        admin: "order:read"
  # 依赖通过 Maven profile "swagger" 引入：mvn ... -Pswagger
  swagger:
    title: Frame Me Tester API
    description: Frame Me Tester 接口文档
    version: 1.0.0
    groups:
      - name: tester-api
        paths-to-match:
          - /api/**
      - name: base-api
        paths-to-match:
          - /base/**
  redis:
    clients:
      second:
        host: localhost
        port: 6379
        database: 1
  # mybatis:
  #   meta-object-handler:
  #     enabled: true
  #   snowflake:
  #     worker-id: 1
  #     datacenter-id: 0
  sse:
    path: /base/sse

jetcache:
  decodeFilterAllowPatterns:
    - com.frame.me.
  statIntervalMinutes: 15
  local:
    default:
      type: caffeine
      keyConvertor: fastjson2
      limit: 100
      expireAfterWriteInMillis: 600000
  remote:
    default:
      type: redis.lettuce
      keyConvertor: fastjson2
      valueEncoder: kryo5
      valueDecoder: kryo5
      expireAfterWriteInMillis: 1800000
      broadcastChannel: ${spring.application.name}
      uri: redis://localhost:6379/0
#      uri: redis://u:p@host:port/db # @符号得替换成编码符

