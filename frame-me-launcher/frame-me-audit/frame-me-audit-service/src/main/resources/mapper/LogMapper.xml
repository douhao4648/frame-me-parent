<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.frame.me.audit.mapper.LogMapper">

    <!--
        详情查询：手动指定结果字段映射，按 ID 查询单条并过滤逻辑删除数据
    -->
    <select id="getById" resultType="com.frame.me.audit.api.vo.LogVO">
        SELECT id, action, category, description, operator_id, target_id, params, result, success, error_msg, duration_ms, timestamp, source_service, target_service, create_time
        FROM audit_log
        WHERE id = #{id}
          AND deleted = 0
    </select>

</mapper>
